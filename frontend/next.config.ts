import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  reactStrictMode: false,
  // Otras configuraciones si las tienes, por ejemplo:
  // eslint: { ignoreDuringBuilds: true },
};

export default nextConfig;